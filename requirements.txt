# Futuro 360 — dependencias de Python (versiones fijadas y verificadas en PyPI).
# Flask 3.0.0 requiere Werkzeug>=3.0; 3.0.1 es compatible.
Flask==3.0.0
Werkzeug==3.0.1
# Conector MySQL (SSL/TLS para Aiven). 8.2.0 publicado en PyPI.
mysql-connector-python==8.2.0
# Carga de variables de entorno desde .env.
python-dotenv==1.0.0
# Envío de correos (Recuperación de contraseña y soporte).
resend==2.0.0
# Cliente HTTP (búsqueda de universidades, envío de PIN vía API de Brevo).
requests==2.34.2
# Exportación de reportes a Excel en el panel admin.
openpyxl==3.1.5
# Generación de PDF del resultado vocacional (WeasyPrint, requiere Pango en el SO).
weasyprint==69.0
# Subida de imágenes/videos a Cloudinary (fallback local sin claves).
cloudinary==1.46.2
# Servidor WSGI para producción (Render). 25.3.0 publicado en PyPI.
# Serie 22.x/23.x también disponibles; 25.3.0 es la que usamos por su soporte.
gunicorn==25.3.0
# Búsqueda web de universidades (DuckDuckGo).
ddgs==9.14.4
# Protección CSRF (tokens por formulario/AJAX). Trae wtforms como dependencia.
flask-wtf==1.2.1
